<Page x:Class="BatteryAging.UI.Pages.DataQueryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:oxy="clr-namespace:OxyPlot.SkiaSharp.Wpf;assembly=OxyPlot.SkiaSharp.Wpf"
      xmlns:ctrl="clr-namespace:BatteryAging.UI.Controls"
      Title="数据查询"
      Background="{StaticResource VsBgDeepBrush}">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 查询条件 -->
        <Border Grid.Row="0" Style="{StaticResource VsCardStyle}" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="起始日期:" VerticalAlignment="Center" Margin="0,0,6,0"
                           Foreground="{StaticResource VsTextSecondaryBrush}"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding StartDate}"/>

                <TextBlock Grid.Column="2" Text="结束日期:" VerticalAlignment="Center" Margin="12,0,6,0"
                           Foreground="{StaticResource VsTextSecondaryBrush}"/>
                <DatePicker Grid.Column="3" SelectedDate="{Binding EndDate}"/>

                <TextBlock Grid.Column="4" Text="电池条码:" VerticalAlignment="Center" Margin="12,0,6,0"
                           Foreground="{StaticResource VsTextSecondaryBrush}"/>
                <TextBox Grid.Column="5" Text="{Binding BarCode, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Column="6" Text="通道:" VerticalAlignment="Center" Margin="12,0,6,0"
                           Foreground="{StaticResource VsTextSecondaryBrush}"/>
                <TextBox Grid.Column="7" Text="{Binding ChannelFilter, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"/>

                <StackPanel Grid.Column="9" Orientation="Horizontal">
                    <Button Style="{StaticResource VsAccentButton}"
                            Command="{Binding QueryCommand}"
                            Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <ctrl:VsIcon Kind="{StaticResource Icon.Magnify}" Width="13" Height="13" Foreground="White"/>
                            <TextBlock Text="查询" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource VsOutlinedButton}"
                            Command="{Binding ExportCommand}"
                            Margin="4,0">
                        <StackPanel Orientation="Horizontal">
                            <ctrl:VsIcon Kind="{StaticResource Icon.FileExportOutline}" Width="13" Height="13"
                                         Foreground="{StaticResource VsAccentBrush}"/>
                            <TextBlock Text="导出 CSV" Margin="6,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 记录列表 -->
        <Border Grid.Row="1" Style="{StaticResource VsCardStyle}" Margin="0,8,0,4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="{StaticResource VsBgHeaderBrush}"
                        BorderBrush="{StaticResource VsBorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="12,6">
                    <TextBlock Text="测试记录" Foreground="{StaticResource VsTextPrimaryBrush}" FontWeight="SemiBold"/>
                </Border>
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Records}"
                          SelectedItem="{Binding SelectedRecord}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="通道" Binding="{Binding ChannelIndex}" Width="50"/>
                        <DataGridTextColumn Header="条码" Binding="{Binding BarCode}" Width="120"/>
                        <DataGridTextColumn Header="方案" Binding="{Binding RecipeName}" Width="160"/>
                        <DataGridTextColumn Header="开始" Binding="{Binding StartTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="140"/>
                        <DataGridTextColumn Header="结束" Binding="{Binding EndTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="140"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                        <DataGridTextColumn Header="保护触发" Binding="{Binding ProtectionTrigger}" Width="120"/>
                        <DataGridTextColumn Header="充电(Ah)" Binding="{Binding TotalChargeCapacity, StringFormat={}{0:F4}}" Width="90"/>
                        <DataGridTextColumn Header="放电(Ah)" Binding="{Binding TotalDischargeCapacity, StringFormat={}{0:F4}}" Width="90"/>
                        <DataGridTextColumn Header="充能量(Wh)" Binding="{Binding TotalChargeEnergy, StringFormat={}{0:F4}}" Width="100"/>
                        <DataGridTextColumn Header="放能量(Wh)" Binding="{Binding TotalDischargeEnergy, StringFormat={}{0:F4}}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- 详细数据 -->
        <Grid Grid.Row="2" Margin="0,4,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource VsCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{StaticResource VsBgHeaderBrush}"
                            BorderBrush="{StaticResource VsBorderBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <ctrl:VsIcon Kind="{StaticResource Icon.ChartLine}" Width="14" Height="14"
                                         Foreground="{StaticResource VsAccentBrush}"/>
                            <TextBlock Text="历史曲线" FontWeight="SemiBold" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <oxy:PlotView Grid.Row="1" x:Name="HistoryPlot" Background="Transparent" Margin="2,4,2,2"  />
                    <oxy:PlotView Grid.Row="2" x:Name="CyclePlot" Background="Transparent" Margin="2,4,2,2"/>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource VsCardStyle}" Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="{StaticResource VsBgHeaderBrush}"
                            BorderBrush="{StaticResource VsBorderBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="12,6">
                        <TextBlock Text="数据点" Foreground="{StaticResource VsTextPrimaryBrush}" FontWeight="SemiBold"/>
                    </Border>
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding DataPoints}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              EnableRowVirtualization="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="时间(s)" Binding="{Binding ElapsedSeconds, StringFormat={}{0:F1}}" Width="70"/>
                            <DataGridTextColumn Header="V" Binding="{Binding Voltage, StringFormat={}{0:F4}}" Width="80"/>
                            <DataGridTextColumn Header="I" Binding="{Binding Current, StringFormat={}{0:F4}}" Width="80"/>
                            <DataGridTextColumn Header="Ah" Binding="{Binding Capacity, StringFormat={}{0:F4}}" Width="80"/>
                            <DataGridTextColumn Header="°C" Binding="{Binding Temperature, StringFormat={}{0:F1}}" Width="60"/>
                            <DataGridTextColumn Header="SOC" Binding="{Binding Soc, StringFormat={}{0:F1}}" Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="3" Padding="8,4" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource VsTextMutedBrush}" FontSize="11"/>
        </Border>
    </Grid>
</Page>
